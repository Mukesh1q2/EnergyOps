# ============================================================================
# OptiBid Energy Platform - Backend Environment Configuration
# Development Environment
# ============================================================================
# This is a development configuration with recommended settings.
# For production deployment, see .env.example for all available options.
# ============================================================================

# ============================================================================
# REQUIRED SERVICES - Must be configured for the application to run
# ============================================================================

# Database Configuration (REQUIRED)
# PostgreSQL is the only required service for core functionality
DATABASE_URL=postgresql+asyncpg://optibid:optibid_password_2025@localhost:5432/optibid

# Security Configuration (REQUIRED)
# IMPORTANT: Change SECRET_KEY to a secure random value in production
# Generate with: openssl rand -hex 32
SECRET_KEY=dev-secret-key-change-in-production-2025-optibid-energy
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Application Configuration (REQUIRED)
ENVIRONMENT=development
DEBUG=true
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# ============================================================================
# OPTIONAL SERVICES - Enable as needed for enhanced functionality
# ============================================================================

# Redis Configuration (OPTIONAL)
# Used for: caching, session management, WebSocket state
# Impact if disabled: Slower performance, in-memory fallbacks used
# Recommended: Enable for production deployments
REDIS_URL=redis://:redis_password_2025@localhost:6379/0
ENABLE_REDIS=false

# WebSocket Configuration (OPTIONAL)
# Used for: Real-time market data updates, live bidding
# Impact if disabled: Clients must poll for updates
# Recommended: Enable for real-time features
ENABLE_WEBSOCKET=false

# Kafka Configuration (OPTIONAL)
# Used for: Real-time market data streaming
# Impact if disabled: Market data polled periodically instead
# Recommended: Enable for high-frequency trading scenarios
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
ENABLE_KAFKA=false

# ClickHouse Configuration (OPTIONAL)
# Used for: High-performance OLAP analytics
# Impact if disabled: Analytics features unavailable or use PostgreSQL
# Recommended: Enable for advanced analytics features
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=8123
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=clickhouse_password_2025
CLICKHOUSE_DATABASE=optibid_analytics
ENABLE_CLICKHOUSE=false

# MLflow Configuration (OPTIONAL)
# Used for: ML model tracking and versioning
# Impact if disabled: ML model management features unavailable
# Recommended: Enable for ML/AI features
MLFLOW_TRACKING_URI=http://localhost:5000
MLFLOW_EXPERIMENT_NAME=optibid_forecasting
ENABLE_MLFLOW=false

# ============================================================================
# FEATURE FLAGS - Control application behavior
# ============================================================================

# Simulation Mode (Development)
# Enable market data simulation for testing without real data sources
SIMULATION_MODE=true
SIMULATION_INTERVAL=5

# ============================================================================
# EXTERNAL SERVICES - Third-party integrations (all optional)
# ============================================================================
# Uncomment and configure as needed:

# Email Configuration (for notifications, password resets)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# EMAIL_FROM=noreply@optibid.io

# Google Maps API (for geocoding and location features)
# GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Error Tracking (Sentry)
# SENTRY_DSN=your-sentry-dsn

# SMS/MFA (Twilio)
# TWILIO_ACCOUNT_SID=your-twilio-account-sid
# TWILIO_AUTH_TOKEN=your-twilio-auth-token
# TWILIO_PHONE_NUMBER=your-twilio-phone-number

# ============================================================================
# DEPLOYMENT NOTES
# ============================================================================
#
# MINIMAL DEPLOYMENT (Current Configuration):
# - PostgreSQL only
# - All optional services disabled
# - Suitable for: Development, testing, basic functionality
#
# RECOMMENDED DEVELOPMENT:
# - Enable ENABLE_REDIS=true for better performance
# - Enable ENABLE_WEBSOCKET=true for real-time features
#
# PRODUCTION DEPLOYMENT:
# - Enable ENABLE_REDIS=true (strongly recommended)
# - Enable ENABLE_WEBSOCKET=true (for real-time features)
# - Configure SMTP for email notifications
# - Set up monitoring (Sentry, Prometheus)
# - Change SECRET_KEY to a secure random value
# - Set DEBUG=false
#
# FULL ENTERPRISE DEPLOYMENT:
# - Enable all optional services (Redis, Kafka, ClickHouse, MLflow)
# - Configure SSO (Azure AD, Okta, Google Workspace)
# - Set up cloud storage (AWS S3) for backups
# - Configure payment processing (Stripe/Razorpay)
# - Enable compliance features (SOC2, GDPR)
#
# For complete configuration options, see .env.example
# ============================================================================
